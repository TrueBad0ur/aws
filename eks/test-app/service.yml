apiVersion: v1
kind: Service
metadata:
  namespace: anime
  name: anime-svc
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: anime-app